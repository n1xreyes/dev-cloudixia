import { ProjectCategoryTitlePipe } from './project-category-title.pipe';

describe('ProjectCategoryTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectCategoryTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
